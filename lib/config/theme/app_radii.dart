/// Corner-radius scale. Bumped up across the board for the warm, rounded
/// "meetgen"-style re-theme — cards and buttons read softer and friendlier.
abstract final class AppRadii {
  static const double sm = 12;
  static const double card = 20;
  static const double lg = 24;
  static const double groupedList = 20;
  static const double pill = 9999;

  const AppRadii._();
}
